import React from 'react';

const Service = () => {
    return (
        <section id='service' style={{ width: "100%", height: "100vh", background: "purple", fontSize: "40px", display: "flex", justifyContent: "center", alignItems: "center", color: "white" }}>
            Service
        </section>
    );
};

export default Service;