import React from 'react';

const Contact = () => {
    return (
        <section id='contact' style={{ width: "100%", height: "100vh", background: "#7633FA", fontSize: "40px", display: "flex", justifyContent: "center", alignItems: "center", color: "white" }}>
            Contact
        </section>
    );
};

export default Contact;