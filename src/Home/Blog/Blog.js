import React from 'react';

const Blog = () => {
    return (
        <section id='blog' style={{ width: "100%", height: "100vh", background: "cyan", fontSize: "40px", display: "flex", justifyContent: "center", alignItems: "center", color: "white" }}>
            Blog
        </section>
    );
};

export default Blog;