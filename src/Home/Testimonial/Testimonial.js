import React from 'react';

const Testimonial = () => {
    return (
        <section id='testimonial' style={{ width: "100%", height: "100vh", background: "olive", fontSize: "40px", display: "flex", justifyContent: "center", alignItems: "center", color: "white" }}>
            Testimonial
        </section>
    );
};

export default Testimonial;