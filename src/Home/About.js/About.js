import React from 'react';

const About = () => {
    return (
        <section id='about' style={{ width: "100%", height: "100vh", background: "green", fontSize: "40px", display: "flex", justifyContent: "center", alignItems: "center", color: "white" }}>
            About
        </section>
    );
};

export default About;