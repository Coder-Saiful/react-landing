import React from 'react';

const Banner = () => {
    return (
        <section style={{ width: "100%", height: "100vh", background: "red", fontSize: "40px", display: "flex", justifyContent: "center", alignItems: "center", color: "white" }}>
            Banner
        </section>
    );
};

export default Banner;