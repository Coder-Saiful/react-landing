import React from 'react';

const Product = () => {
    return (
        <section id='product' style={{ width: "100%", height: "100vh", background: "orange", fontSize: "40px", display: "flex", justifyContent: "center", alignItems: "center", color: "white" }}>
            Product
        </section>
    );
};

export default Product;