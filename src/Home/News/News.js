import React from 'react';

const News = () => {
    return (
        <section id='news' style={{ width: "100%", height: "100vh", background: "SteelBlue", fontSize: "40px", display: "flex", justifyContent: "center", alignItems: "center", color: "white" }}>
            News
        </section>
    );
};

export default News;